<div class="flex items-center justify-center min-h-screen bg-gray-900">
  <div class="w-full max-w-md bg-gray-800 rounded-lg shadow-xl p-8">
    <h1 class="text-xl font-bold text-white mb-1">Create Channel</h1>
    <p class="text-sm text-gray-400 mb-6">in <%= @server.name %></p>

    <%= form_with model: [@server, @channel], class: "space-y-4" do |f| %>
      <div>
        <label class="block text-xs font-semibold text-gray-300 uppercase tracking-wide mb-1">Channel Type</label>
        <%= f.select :channel_type, Channel.channel_types.keys.map { |t| [t.capitalize, t] }, {}, class: "w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white focus:outline-none focus:border-accent text-sm" %>
      <div>
        <label class="block text-xs font-semibold text-gray-300 uppercase tracking-wide mb-1">Category</label>
        <%= f.collection_select :category_id, @server.categories.ordered, :id, :name, { include_blank: "No Category" }, class: "w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white focus:outline-none focus:border-accent text-sm" %>
      </div>

      <div data-controller="emoji-input" data-emoji-input-server-id-value="<%= @server.public_id %>">
        <%= f.label :name, "Channel Name", class: "block text-xs font-semibold text-gray-300 uppercase tracking-wide mb-1" %>
        <div class="flex items-center bg-gray-900 border border-gray-700 rounded px-3 py-2">
          <span class="text-gray-500 mr-1">#</span>
          <div class="flex-1 relative">
            <div data-emoji-input-target="highlight" class="absolute inset-0 text-sm text-white whitespace-pre break-words pointer-events-none overflow-hidden" aria-hidden="true"></div>
            <%= f.text_field :name, class: "w-full bg-transparent text-transparent caret-white placeholder-gray-500 focus:outline-none text-sm relative z-10", placeholder: "new-channel", data: { emoji_input_target: "input", action: "input->emoji-input#handleInput keydown->emoji-input#handleKeydown" } %>
          </div>
          <button type="button" class="text-gray-400 hover:text-gray-200 ml-1 shrink-0 cursor-pointer" data-action="click->emoji-input#toggle" data-emoji-input-target="button" title="Add emoji">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          </button>
        </div>
      </div>

      <div>
        <%= f.label :topic, class: "block text-xs font-semibold text-gray-300 uppercase tracking-wide mb-1" %>
        <%= f.text_field :topic, class: "w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white focus:outline-none focus:border-accent text-sm", placeholder: "What's this channel about?" %>
      </div>

      <div class="flex items-center gap-3 pt-2">
        <%= link_to "Cancel", server_channel_path(@server, @server.channels.ordered.first), class: "text-sm text-gray-400 hover:text-gray-200" %>
        <%= f.submit "Create Channel", class: "bg-gradient-to-r from-confirm-dark to-confirm hover:from-confirm hover:to-confirm-light text-white font-semibold px-6 py-2 rounded transition cursor-pointer text-sm" %>
      </div>
    <% end %>
  </div>
</div>
